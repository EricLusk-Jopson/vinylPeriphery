the first 70 lines of each search function are the same.
these should be abstracted into a new method responsible for
retrieving the given release record.

improve url appending

add typescript support
